--- a/drivers/net/ethernet/mediatek/mtk_hnat/hnat.c	2026-07-06 15:34:47.622144782 +0800
+++ b/drivers/net/ethernet/mediatek/mtk_hnat/hnat.c	2026-07-06 15:33:55.665600314 +0800
@@ -1679,6 +1679,12 @@
 	if (err)
 		goto err_out2;
 
+	hnat_priv->nf_stat_en = true; /* enable nf_stat_en by default */
+	hnat_priv->ipv6_en = true; /* enable ipv6 by default */
+	hnat_priv->guest_en = true; /* enable guest wifi by default */
+	hnat_priv->dscp_en = false;
+	hnat_priv->macvlan_support = false;
+
 	prop = of_find_property(np, "ext-devices", NULL);
 	for (name = of_prop_next_string(prop, NULL); name;
 	     name = of_prop_next_string(prop, name), index++) {
--- a/drivers/net/ethernet/mediatek/mtk_hnat/hnat.h	2026-07-03 14:04:15.578206834 +0800
+++ b/drivers/net/ethernet/mediatek/mtk_hnat/hnat.h	2026-07-06 15:33:55.665600314 +0800
@@ -1089,6 +1089,10 @@
 	struct hnat_prot_3t_cfg prot_3t[MAX_PPE_NUM]; /* for 3-tuple offload */
 	struct hnat_neigh_update neigh_update;
 	int fe_irq2;
+	bool ipv6_en;
+	bool guest_en;
+	bool dscp_en;
+	bool macvlan_support;
 };
 
 struct hnat_flow_entry {
@@ -1328,7 +1332,8 @@
 #define LAN_DEV_NAME hnat_priv->lan
 #define LAN2_DEV_NAME hnat_priv->lan2
 #define IS_ETH_GRP(dev) (IS_LAN_GRP(dev) || IS_WAN(dev))
-#define IS_WAN(dev) (!strncmp((dev)->name, WAN_DEV_NAME, strlen(WAN_DEV_NAME)))
+#define IS_WAN(dev) ((!strncmp((dev)->name, WAN_DEV_NAME, strlen(WAN_DEV_NAME))) || \
+		IS_BOND(dev))
 #define IS_LAN_GRP(dev) (IS_LAN(dev) | IS_LAN2(dev))
 #define IS_LAN(dev)								\
 	(!strncmp(dev->name, LAN_DEV_NAME, strlen(LAN_DEV_NAME)) ||		\
--- a/drivers/net/ethernet/mediatek/mtk_hnat/hnat_debugfs.c	2026-07-03 14:04:15.582036664 +0800
+++ b/drivers/net/ethernet/mediatek/mtk_hnat/hnat_debugfs.c	2026-07-06 15:33:55.665600314 +0800
@@ -885,6 +885,12 @@
 	pr_info("              7     0~1        Set hnat counter update to nf_conntrack\n");
 	pr_info("              8     0~6        Set PPE hash simple mode\n");
 	pr_info("              10    0 or 10    Set PPE default cpu port for GMACs\n");
+	pr_info("              -- Extended turboacc commands --\n");
+	pr_info("              11    0~1        Set hnat disable/enable ipv6\n");
+	pr_info("              12    0~1        Set hnat disable/enable guest (rax1/ra1)\n");
+	pr_info("              13    0~1        Set hnat disable/enable dscp setting\n");
+	pr_info("              14    1~30       Set hnat bind rate\n");
+	pr_info("              15    0~1        Set hnat macvlan support mode\n");
 
 	return 0;
 }
@@ -1046,6 +1052,119 @@
 	return 0;
 }
 
+static int set_ipv6_toggle(int toggle)
+{
+	struct mtk_hnat *h = hnat_priv;
+
+	if (toggle == 1)
+		pr_info("Enable hnat ipv6\n");
+	else if (toggle == 0)
+		pr_info("Disable hnat ipv6\n");
+	else {
+		pr_info("input error, current ipv6_en=%d\n", h->ipv6_en);
+		return 0;
+	}
+	h->ipv6_en = toggle;
+
+	return 0;
+}
+
+static int set_guest_toggle(int toggle)
+{
+	struct mtk_hnat *h = hnat_priv;
+	struct net_device *dev;
+
+	if (toggle == 1)
+		pr_info("Enable hnat guest interface\n");
+	else if (toggle == 0)
+		pr_info("Disable hnat guest interface\n");
+	else {
+		pr_info("input error, current guest_en=%d\n", h->guest_en);
+		return 0;
+	}
+	h->guest_en = toggle;
+
+	/* Register/unregister guest wifi interfaces */
+	dev = dev_get_by_name(&init_net, "ra1");
+	if (dev) {
+		if (toggle)
+			mtk_ppe_dev_register_hook(dev);
+		else
+			mtk_ppe_dev_unregister_hook(dev);
+		dev_put(dev);
+	}
+	dev = dev_get_by_name(&init_net, "rax1");
+	if (dev) {
+		if (toggle)
+			mtk_ppe_dev_register_hook(dev);
+		else
+			mtk_ppe_dev_unregister_hook(dev);
+		dev_put(dev);
+	}
+	dev = dev_get_by_name(&init_net, "rai1");
+	if (dev) {
+		if (toggle)
+			mtk_ppe_dev_register_hook(dev);
+		else
+			mtk_ppe_dev_unregister_hook(dev);
+		dev_put(dev);
+	}
+
+	return 0;
+}
+
+static int set_dscp_toggle(int toggle)
+{
+	struct mtk_hnat *h = hnat_priv;
+
+	if (toggle == 1)
+		pr_info("Enable hqos dscp setting\n");
+	else if (toggle == 0)
+		pr_info("Disable hqos dscp setting\n");
+	else {
+		pr_info("input error, current hqos dscp setting=%d\n", h->dscp_en);
+		return 0;
+	}
+	h->dscp_en = toggle;
+
+	return 0;
+}
+
+static int bind_rate_setting(int bind_rate)
+{
+	int i;
+
+	if ((bind_rate > 30) || (bind_rate < 1)) {
+		bind_rate = 30;
+		pr_info("bind_rate max interval = 30\n");
+	} else {
+		pr_info("bind_rate = %d\n", bind_rate);
+	}
+
+	/* Set bind rate for FOE entries */
+	for (i = 0; i < CFG_PPE_NUM; i++)
+		cr_set_field(hnat_priv->ppe_base[i] + PPE_BNDR, BIND_RATE, bind_rate);
+
+	return 0;
+}
+
+static int set_macvlan_support(int toggle)
+{
+	struct mtk_hnat *h = hnat_priv;
+
+	if (toggle == 1)
+		pr_info("Enable macvlan support\n");
+	else if (toggle == 0)
+		pr_info("Disable macvlan support\n");
+	else {
+		pr_info("input error, current macvlan support setting=%d\n", h->macvlan_support);
+		return 0;
+	}
+	h->macvlan_support = toggle;
+
+	return 0;
+}
+
 static int set_default_cpu_port(int port)
 {
 	bool is_v3 = hnat_priv->data->version == MTK_HNAT_V3;
@@ -1094,6 +1213,9 @@
 	[6] = udp_keep_alive,       [7] = set_nf_update_toggle,
 	[8] = set_hash_simple_mode, [9] = cr_set_usage,
 	[10] = set_default_cpu_port,
+	[11] = set_ipv6_toggle,     [12] = set_guest_toggle,
+	[13] = set_dscp_toggle,     [14] = bind_rate_setting,
+	[15] = set_macvlan_support,
 };
 
 static int read_mib(struct mtk_hnat *h, u32 ppe_id,
@@ -2118,6 +2240,30 @@
 	}
 }
 
+static int hnat_stats_read(struct seq_file *m, void *private)
+{
+	struct mtk_hnat *h = hnat_priv;
+	struct foe_entry *entry, *end;
+	int cnt, i;
+
+	seq_printf(m, "PPE_NUM=%d\n", CFG_PPE_NUM);
+
+	for (i = 0; i < CFG_PPE_NUM; i++) {
+		cnt = 0;
+		entry = h->foe_table_cpu[i];
+		end = h->foe_table_cpu[i] + hnat_priv->foe_etry_num;
+		while (entry < end) {
+			if (entry->bfib1.state == BIND)
+				cnt++;
+			entry++;
+		}
+		seq_printf(m, "ALL_PPE%d=%d\n", i, hnat_priv->foe_etry_num);
+		seq_printf(m, "BIND_PPE%d=%d\n", i, cnt);
+	}
+
+	return 0;
+}
+
 static int __hnat_entry_read(struct seq_file *m, void *private, u32 ppe_id)
 {
 	struct mtk_hnat *h = hnat_priv;
@@ -2250,6 +2396,18 @@
 	.release = single_release,
 };
 
+static int hnat_stats_open(struct inode *inode, struct file *file)
+{
+	return single_open(file, hnat_stats_read, file->private_data);
+}
+
+static const struct file_operations hnat_stats_fops = {
+	.open = hnat_stats_open,
+	.read = seq_read,
+	.llseek = seq_lseek,
+	.release = single_release,
+};
+
 static int __hnat_setting_read(struct seq_file *m, void *private, u32 ppe_id)
 {
 	struct mtk_hnat *h = hnat_priv;
@@ -2327,6 +2485,11 @@
 	case 8:
 	case 9:
 	case 10:
+	case 11:
+	case 12:
+	case 13:
+	case 14:
+	case 15:
 		p_token = strsep(&p_buf, p_delimiter);
 		if (!p_token)
 			arg1 = 0;
@@ -2477,8 +2640,8 @@
 	for (i = 0; i < MAX_EXT_DEVS && hnat_priv->ext_if[i]; i++) {
 		ext_entry = hnat_priv->ext_if[i];
 		if (ext_entry->dev)
-			seq_printf(m, "ext devices [%d] = %s  (dev=%p, ifindex=%d)\n",
-				   i, ext_entry->name, ext_entry->dev,
+			seq_printf(m, "ext devices [%d] = %s,  (dev=%p, ifindex=%d)\n",
+				   i, ext_entry->dev->name, ext_entry->dev,
 				   ext_entry->dev->ifindex);
 	}
 
@@ -2613,7 +2776,7 @@
 
 static int hnat_hook_toggle_read(struct seq_file *m, void *private)
 {
-	pr_info("value=%d, hook is %s now!\n", hook_toggle, (hook_toggle) ? "enabled" : "disabled");
+	seq_printf(m, "%s\n", (hook_toggle) ? "enabled" : "disabled");
 
 	return 0;
 }
@@ -4076,6 +4239,8 @@
 			    &cpu_reason_fops);
 	debugfs_create_file("hnat_entry", 0444, root, h,
 			    &hnat_entry_fops);
+	debugfs_create_file("hnat_stats", 0444, root, h,
+			    &hnat_stats_fops);
 	debugfs_create_file("hnat_setting", 0444, root, h,
 			    &hnat_setting_fops);
 	debugfs_create_file("mcast_table", 0444, root, h,
--- a/drivers/net/ethernet/mediatek/mtk_hnat/hnat_nf_hook.c	2026-07-06 15:34:47.618373641 +0800
+++ b/drivers/net/ethernet/mediatek/mtk_hnat/hnat_nf_hook.c	2026-07-06 15:33:55.673276168 +0800
@@ -417,6 +417,14 @@
 
 	switch (event) {
 	case NETDEV_UP:
+		if (!hnat_priv->guest_en) {
+			if (!strcmp(dev->name, "ra1") ||
+			    !strcmp(dev->name, "rax1") ||
+			    !strcmp(dev->name, "rai1")) {
+				break;
+			}
+		}
+
 		gmac_ppe_fwd_enable(dev);
 
 		extif_set_dev(dev);
--- a/include/net/netfilter/nf_conntrack_acct.h	2026-06-19 19:42:39.000000000 +0800
+++ b/include/net/netfilter/nf_conntrack_acct.h	2026-07-06 15:33:55.673276168 +0800
@@ -14,6 +14,8 @@
 struct nf_conn_counter {
 	atomic64_t packets;
 	atomic64_t bytes;
+	atomic64_t diff_packets;
+	atomic64_t diff_bytes;
 };
 
 struct nf_conn_acct {
--- a/include/net/netfilter/nf_flow_table.h	2026-07-06 15:34:47.822015224 +0800
+++ b/include/net/netfilter/nf_flow_table.h	2026-07-06 15:33:55.673276168 +0800
@@ -217,6 +217,7 @@
 	u16 vlan_id;
 	u16 pppoe_sid;
 	u16 dsa_port;
+	u32 skb_hash;
 };
 
 #define NF_FLOW_TIMEOUT (30 * HZ)
